#include <iostream>
#include <string>
#include <cmath>
using namespace std;

// We always use (while loops) when we didn't know the ranges of the task (from what to what)

int main()
{
    int i = 1;
    while (i <= 5)
    {
        cout << i << endl;
        i++;
    }

    return 0;
}