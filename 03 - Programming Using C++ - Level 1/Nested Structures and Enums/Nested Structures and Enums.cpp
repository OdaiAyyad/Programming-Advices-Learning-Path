#include <iostream>
#include <cmath>
using namespace std;

enum enColor { Red, Green, Blue };
enum enGender { Male, Female };
enum enMartialStatus { Single, Married };

struct stAddress
{

};

int main()
{


    return 0;
}